set(PLEGMA_LIBS plegma PLEGMAutils ${CMAKE_THREAD_LIBS_INIT} ${QUDA_LIB} ${MPI_CXX_LIBRARIES} ${HDF5_DIR}/lib//libhdf5_parallel.so.200 -fopenmp )
#opt/cray/pe/hdf5-parallel/1.12.1.5/gnu/9.1/lib/libhdf5_parallel.so.200 ${MPI_CXX_LIBRARIES} /pfs/lustrep2/projappl/project_462000125/samantao-public/rocm/rocm-5.3.3/hip/lib/libamdhip64.so)

LIST(APPEND PLEGMA_LIBS ${MPI_CXX_LIBRARIES})

if(PLEGMA_ARPACK)
  LIST(APPEND PLEGMA_LIBS ${PLEGMA_MPIFH_LIB})
  LIST(APPEND PLEGMA_LIBS ${PLEGMA_Z_LIB})
  LIST(APPEND PLEGMA_LIBS ${PLEGMA_GFORTRAN_LIB})
  LIST(APPEND PLEGMA_LIBS ${PLEGMA_PARPACK_LIB})
  LIST(APPEND PLEGMA_LIBS ${PLEGMA_ARPACK_LIB})
endif()

if(PLEGMA_PRIMME)
  LIST(APPEND PLEGMA_LIBS ${PLEGMA_GFORTRAN_LIB})
  LIST(APPEND PLEGMA_LIBS ${PLEGMA_PRIMME_LIB})
endif()

LIST(APPEND PLEGMA_LIBS ${PLEGMA_GSL_LIB})
LIST(APPEND PLEGMA_LIBS ${HDF5_C_LIBRARY_hdf5})
LIST(APPEND PLEGMA_LIBS ${PLEGMA_LIME_LIB})

if(PLEGMA_OPENBLAS)
  LIST(APPEND PLEGMA_LIBS ${PLEGMA_OPENBLAS_GOMP})
  LIST(APPEND PLEGMA_LIBS ${PLEGMA_OPENBLAS_LIB})
endif(PLEGMA_OPENBLAS)

if(PLEGMA_MKL)
  LIST(APPEND PLEGMA_LIBS ${PLEGMA_MKL_LIB_GFORTRAN})
  LIST(APPEND PLEGMA_LIBS ${PLEGMA_MKL_LIB_IOMP5})
  LIST(APPEND PLEGMA_LIBS ${PLEGMA_MKL_LIB_LP64})
  LIST(APPEND PLEGMA_LIBS ${PLEGMA_MKL_LIB_THREAD})
  LIST(APPEND PLEGMA_LIBS ${PLEGMA_MKL_LIB_CORE})
endif(PLEGMA_MKL)

#define PLEGMA executables
#if(NOT DEFINED(HCC_HOME))
#  set(HCC_HOME "${HIP_PATH}/../llvm/bin")
#endif()
#set(CMAKE_HIP_LINK_EXECUTABLE "${HIP_HIPCC_CMAKE_LINKER_HELPER} ${HCC_HOME} <FLAGS> <CMAKE_CXX_LINK_FLAGS> <LINK_FLAGS> <OBJECTS> -o <TARGET> <LINK_LIBRARIES>")
#set(CMAKE_HIP_LINK_EXECUTABLE "${HIP_HIPCC_CMAKE_LINKER_HELPER} ${HIP_CLANG_PATH} ${HIP_CLANG_PARALLEL_BUILD_LINK_OPTIONS} <FLAGS> <CMAKE_CXX_LINK_FLAGS> <LINK_FLAGS> <OBJECTS> -o <TARGET> <LINK_LIBRARIES>")
#set(HIPCC "${HIP_PATH}/bin/hipcc")
if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  find_package(HIPWrapper)
  hip_add_executable(Plaquette Plaquette.cpp)
  set_property(TARGET Plaquette PROPERTY HIP_ARCHITECTURES gfx90a)
  message("Stop ${CMAKE_HIP_COMPILE_OBJECT}")
  message("SAS ${CMAKE_HIP_LINK_EXECUTABLE}")
  #set_source_files_properties(Plaquette.cpp PROPERTIES LANGUAGE HIP)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(Plaquette Plaquette.cpp)
endif()
target_link_libraries(Plaquette ${PLEGMA_LIBS})
if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(Topo Topo.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(Topo Topo.cpp)
endif()
target_link_libraries(Topo ${PLEGMA_LIBS})

if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(Calc_2pt Calc_2pt.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(Calc_2pt Calc_2pt.cpp)
endif()
target_link_libraries(Calc_2pt ${PLEGMA_LIBS})
if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(Calc_2pt_marconi Calc_2pt_marconi.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(Calc_2pt_marconi Calc_2pt_marconi.cpp)
endif()
target_link_libraries(Calc_2pt_marconi ${PLEGMA_LIBS})

if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(Calc_2pt_wilson Calc_2pt_wilson.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(Calc_2pt_wilson Calc_2pt_wilson.cpp)
endif()
target_link_libraries(Calc_2pt_wilson ${PLEGMA_LIBS})

if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(Tetraquark Tetraquark.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(Tetraquark Tetraquark.cpp)
endif()
target_link_libraries(Tetraquark ${PLEGMA_LIBS})

if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(TetraquarkBCUD TetraquarkBCUD.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(TetraquarkBCUD TetraquarkBCUD.cpp)
endif()
target_link_libraries(TetraquarkBCUD ${PLEGMA_LIBS})

if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(TestFrame EXCLUDE_FROM_ALL TestFrame.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(TestFrame EXCLUDE_FROM_ALL TestFrame.cpp)
endif()
target_link_libraries(TestFrame ${PLEGMA_LIBS})

if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP") 
  hip_add_executable(ComputeLightStochProp ComputeLightStochProp.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(ComputeLightStochProp ComputeLightStochProp.cpp)
endif()
target_link_libraries(ComputeLightStochProp ${PLEGMA_LIBS})

if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(StochasticTetraquark StochasticTetraquark.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(StochasticTetraquark StochasticTetraquark.cpp)
endif()
target_link_libraries(StochasticTetraquark ${PLEGMA_LIBS})

if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(StochasticTetraquarkBCUD StochasticTetraquarkBCUD.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(StochasticTetraquarkBCUD StochasticTetraquarkBCUD.cpp)
endif()
target_link_libraries(StochasticTetraquarkBCUD ${PLEGMA_LIBS})


if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(ComputeLightPointProp ComputeLightPointProp.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(ComputeLightPointProp ComputeLightPointProp.cpp)
endif()
target_link_libraries(ComputeLightPointProp ${PLEGMA_LIBS})
if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(Calc_2pt_charmonium Calc_2pt_charmonium.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(Calc_2pt_charmonium Calc_2pt_charmonium.cpp)
endif()
target_link_libraries(Calc_2pt_charmonium ${PLEGMA_LIBS})

if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(nucleon_2pt_3pt nucleon_2pt_3pt.cpp) 
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(nucleon_2pt_3pt nucleon_2pt_3pt.cpp)
endif()
target_link_libraries(nucleon_2pt_3pt ${PLEGMA_LIBS})
if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(PDFs_nucleon PDFs_nucleon.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(PDFs_nucleon PDFs_nucleon.cpp)
endif()
target_link_libraries(PDFs_nucleon ${PLEGMA_LIBS})

if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(TMDPDFs_nucleon TMDPDFs_nucleon.cpp) 
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(TMDPDFs_nucleon TMDPDFs_nucleon.cpp)
endif()
target_link_libraries(TMDPDFs_nucleon ${PLEGMA_LIBS})
if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(PDFs_qgq_nucleon PDFs_qgq_nucleon.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(PDFs_qgq_nucleon PDFs_qgq_nucleon.cpp)
endif()
target_link_libraries(PDFs_qgq_nucleon ${PLEGMA_LIBS})

if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(nucleon_2pt_3pt_npi nucleon_2pt_3pt_npi.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(nucleon_2pt_3pt_npi nucleon_2pt_3pt_npi.cpp)
endif()
target_link_libraries(nucleon_2pt_3pt_npi ${PLEGMA_LIBS})

if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(mesons_3pt mesons_3pt.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(mesons_3pt mesons_3pt.cpp)
endif()
target_link_libraries(mesons_3pt ${PLEGMA_LIBS})

if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(correcting_ZM EXCLUDE_FROM_ALL correcting_ZM.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(correcting_ZM EXCLUDE_FROM_ALL correcting_ZM.cpp)
endif()
target_link_libraries(correcting_ZM ${PLEGMA_LIBS})

if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(Source EXCLUDE_FROM_ALL Source.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(Source EXCLUDE_FROM_ALL Source.cpp)
endif()
target_link_libraries(Source ${PLEGMA_LIBS})

if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(Random EXCLUDE_FROM_ALL Random.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(Random EXCLUDE_FROM_ALL Random.cpp)
endif()
target_link_libraries(Random ${PLEGMA_LIBS})

if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(Dilution EXCLUDE_FROM_ALL Dilution.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(Dilution EXCLUDE_FROM_ALL Dilution.cpp)
endif()
target_link_libraries(Dilution ${PLEGMA_LIBS})

if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(testLinksSmearings EXCLUDE_FROM_ALL testLinksSmearings.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(testLinksSmearings EXCLUDE_FROM_ALL testLinksSmearings.cpp)
endif()
target_link_libraries(testLinksSmearings ${PLEGMA_LIBS})

if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(testHDF5 EXCLUDE_FROM_ALL testHDF5.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(testHDF5 EXCLUDE_FROM_ALL testHDF5.cpp)
endif()
target_link_libraries(testHDF5 ${PLEGMA_LIBS})

if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(qLoops qLoops.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(qLoops qLoops.cpp)
endif()
target_link_libraries(qLoops ${PLEGMA_LIBS})

if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(qLoops_WilsonLine qLoops_WilsonLine.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(qLoops_WilsonLine qLoops_WilsonLine.cpp)
endif()
target_link_libraries(qLoops_WilsonLine ${PLEGMA_LIBS})
if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(FT EXCLUDE_FROM_ALL FT.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(FT EXCLUDE_FROM_ALL FT.cpp)
endif()
target_link_libraries(FT ${PLEGMA_LIBS})

if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(LowModesCalc LowModesCalc.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(LowModesCalc LowModesCalc.cpp)
endif()
target_link_libraries(LowModesCalc ${PLEGMA_LIBS})

if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(MG_tuner MG_tuner.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(MG_tuner MG_tuner.cpp)
endif()
target_link_libraries(MG_tuner ${PLEGMA_LIBS})
if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(benchmark benchmark.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(benchmark benchmark.cpp)
endif()
target_link_libraries(benchmark ${PLEGMA_LIBS})

if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(TMDWFs_mesons  TMDWFs_mesons.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(TMDWFs_mesons  TMDWFs_mesons.cpp)
endif()
target_link_libraries(TMDWFs_mesons ${PLEGMA_LIBS})
if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(quark_rms EXCLUDE_FROM_ALL quark_rms.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(quark_rms EXCLUDE_FROM_ALL quark_rms.cpp)
endif()
target_link_libraries(quark_rms ${PLEGMA_LIBS})

if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(Zfac_Vgg EXCLUDE_FROM_ALL Zfac_Vgg.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(Zfac_Vgg EXCLUDE_FROM_ALL Zfac_Vgg.cpp)
endif()
target_link_libraries(Zfac_Vgg ${PLEGMA_LIBS})

if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(testShifts EXCLUDE_FROM_ALL testShifts.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(testShifts EXCLUDE_FROM_ALL testShifts.cpp)
endif()
target_link_libraries(testShifts ${PLEGMA_LIBS})

if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(Zfac Zfac.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(Zfac Zfac.cpp)
endif()
target_link_libraries(Zfac ${PLEGMA_LIBS})

if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(ZfacStaple ZfacStaple.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(ZfacStaple ZfacStaple.cpp)
endif()
target_link_libraries(ZfacStaple ${PLEGMA_LIBS})

if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(TMDPDFs_mesons_Juwels TMDPDFs_mesons_Juwels.cpp)
endif()
target_link_libraries(TMDPDFs_mesons_Juwels ${PLEGMA_LIBS})

if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(new_TMDPDFs_mesons_Juwels new_TMDPDFs_mesons_Juwels.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(new_TMDPDFs_mesons_Juwels new_TMDPDFs_mesons_Juwels.cpp)
endif() 
target_link_libraries(new_TMDPDFs_mesons_Juwels ${PLEGMA_LIBS})

if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(SoftFunction SoftFunction.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(SoftFunction SoftFunction.cpp)
endif()
target_link_libraries(SoftFunction ${PLEGMA_LIBS})

if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(SoftFunctionWall SoftFunctionWall.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(SoftFunctionWall SoftFunctionWall.cpp)
endif()
target_link_libraries(SoftFunctionWall ${PLEGMA_LIBS})

if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(WallSourceNucleon WallSourceNucleon.cpp) 
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(WallSourceNucleon WallSourceNucleon.cpp)
endif()
target_link_libraries(WallSourceNucleon ${PLEGMA_LIBS})
if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(TMDPDFs_pion TMDPDFs_pion.cpp) 
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(TMDPDFs_pion TMDPDFs_pion.cpp)
endif()
target_link_libraries(TMDPDFs_pion ${PLEGMA_LIBS})

if (PLEGMA_SCATTERING_CONTRACTIONS)
  if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
    hip_add_executable(testScattReductions EXCLUDE_FROM_ALL testScattReductions.cpp)
  endif()
  if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
    cuda_add_executable(testScattReductions EXCLUDE_FROM_ALL testScattReductions.cpp)
  endif()
  target_link_libraries(testScattReductions ${PLEGMA_LIBS})
  if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
    cuda_add_executable(piNdiagrams piNdiagrams.cpp)
  endif()
  if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
    hip_add_executable(piNdiagrams piNdiagrams.cpp)
  endif()
  target_link_libraries(piNdiagrams ${PLEGMA_LIBS})
  if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
    hip_add_executable(nucleon_delta_test EXCLUDE_FROM_ALL nucleon_delta_test.cpp)
  endif()
  if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
    cuda_add_executable(nucleon_delta_test EXCLUDE_FROM_ALL nucleon_delta_test.cpp)
  endif()
  target_link_libraries(nucleon_delta_test ${PLEGMA_LIBS})
  if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
    hip_add_executable(piNdiagrams_oet EXCLUDE_FROM_ALL piNdiagrams_oet.cpp)
  endif()
  if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
    cuda_add_executable(piNdiagrams_oet EXCLUDE_FROM_ALL piNdiagrams_oet.cpp)
  endif()
  target_link_libraries(piNdiagrams_oet ${PLEGMA_LIBS})
  if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
    hip_add_executable(nucleon_3pt_n_npi EXCLUDE_FROM_ALL nucleon_3pt_n_npi.cpp)
  endif()
  if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
    cuda_add_executable(nucleon_3pt_n_npi EXCLUDE_FROM_ALL nucleon_3pt_n_npi.cpp)
  endif()
  target_link_libraries(nucleon_3pt_n_npi ${PLEGMA_LIBS})

  if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
    cuda_add_executable(nucleon_3pt_n_npi_wd1ii EXCLUDE_FROM_ALL nucleon_3pt_n_npi_wd1ii.cpp)
  endif()
  target_link_libraries(nucleon_3pt_n_npi_wd1ii ${PLEGMA_LIBS})
  if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
    hip_add_executable(checking_Doet checking_Doet.cpp)
  endif()
  if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
    cuda_add_executable(checking_Doet checking_Doet.cpp)
  endif()
  target_link_libraries(checking_Doet ${PLEGMA_LIBS})
  if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
    hip_add_executable(omegamass EXCLUDE_FROM_ALL omegamass.cpp)
  endif()
  if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
    cuda_add_executable(omegamass EXCLUDE_FROM_ALL omegamass.cpp)
  endif()
  target_link_libraries(omegamass ${PLEGMA_LIBS})
  if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
    cuda_add_executable(pi0Insertion EXCLUDE_FROM_ALL pi0Insertion.cpp)
  endif()
  target_link_libraries(pi0Insertion ${PLEGMA_LIBS})
  if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
    cuda_add_executable(QuarkLoops_pi0_insertion EXCLUDE_FROM_ALL QuarkLoops_pi0_insertion.cpp)
  endif()
  target_link_libraries(QuarkLoops_pi0_insertion ${PLEGMA_LIBS})
  if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
    cuda_add_executable(testYan EXCLUDE_FROM_ALL testYan.cpp)
  endif()
  target_link_libraries(testYan ${PLEGMA_LIBS})
endif(PLEGMA_SCATTERING_CONTRACTIONS)

if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(gaugeFixing gaugeFixing.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(gaugeFixing gaugeFixing.cpp)
endif()
target_link_libraries(gaugeFixing ${PLEGMA_LIBS})

if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(invert invert.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(invert invert.cpp)
endif()
target_link_libraries(invert ${PLEGMA_LIBS})

if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(polarN EXCLUDE_FROM_ALL polarN.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(polarN EXCLUDE_FROM_ALL polarN.cpp)
endif()
target_link_libraries(polarN ${PLEGMA_LIBS})

if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(Zfac_GI Zfac_GI.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(Zfac_GI Zfac_GI.cpp)
endif()

target_link_libraries(Zfac_GI ${PLEGMA_LIBS})

if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(Zfac_GI_meson3pt Zfac_GI_meson3pt.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(Zfac_GI_meson3pt Zfac_GI_meson3pt.cpp)
endif()
target_link_libraries(Zfac_GI_meson3pt ${PLEGMA_LIBS})

if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(mesons_2pt  mesons_2pt.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(mesons_2pt  mesons_2pt.cpp)
endif()
target_link_libraries(mesons_2pt ${PLEGMA_LIBS})

if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(mesons_simple mesons_simple.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(mesons_simple mesons_simple.cpp)
endif()
target_link_libraries(mesons_simple ${PLEGMA_LIBS})

if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(Zfac_diagGI Zfac_diagGI.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(Zfac_diagGI Zfac_diagGI.cpp)
endif()
target_link_libraries(Zfac_diagGI ${PLEGMA_LIBS})
#if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
#  hip_add_executable(eigVecs eigVecs.cpp)
#endif()
#if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
#  cuda_add_executable(eigVecs eigVecs.cpp)
#endif()
#target_link_libraries(eigVecs ${PLEGMA_LIBS})
if(${PLEGMA_TARGET_TYPE} STREQUAL "HIP")
  hip_add_executable(pion_LowModeAvg pion_LowModeAvg.cpp)
endif()
if(${PLEGMA_TARGET_TYPE} STREQUAL "CUDA")
  cuda_add_executable(pion_LowModeAvg pion_LowModeAvg.cpp)
endif()
target_link_libraries(pion_LowModeAvg ${PLEGMA_LIBS})
